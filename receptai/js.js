document.addEventListener('DOMContentLoaded', () => {

    let recipes = document.querySelectorAll('.receptas');

    recipes.forEach((recipe, index) =>
         recipe.addEventListener('click', () => {
       if (recipe);





    }
    ))

});